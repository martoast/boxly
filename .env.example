
# eBay Browse API — real secondhand prices for the panel's "Usado" section.
# From developer.ebay.com → Application Keysets → Production.
#   EBAY_CLIENT_ID     = the keyset's "App ID"
#   EBAY_CLIENT_SECRET = the keyset's "Cert ID"  (secret)
# Unset = the section falls back to the SerpAPI resale index, as today.
EBAY_CLIENT_ID=
EBAY_CLIENT_SECRET=
EBAY_ENV=production

# Best Buy Products API — verified retail prices for electronics.
# Free key, no approval: developer.bestbuy.com
# Unset = no Best Buy rows, panel behaves as today.
BESTBUY_API_KEY=

# eBay Marketplace Account Deletion (required for production compliance).
# The token is ours to choose: 32-80 chars, [A-Za-z0-9_-]. Must match eBay's
# console exactly. The endpoint is hashed as-given — trailing slash matters.
EBAY_VERIFICATION_TOKEN=
EBAY_DELETION_ENDPOINT=https://boxly.mx/api/ebay/account-deletion
